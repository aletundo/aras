spring:
  output:
    ansi:
      enabled: DETECT
  application:
    name: analyses-configurator-service
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  data:
    mongodb:
      uri: "mongodb://localhost:27017/analyses-configurator"
      
eureka:
  client:
    enabled: false