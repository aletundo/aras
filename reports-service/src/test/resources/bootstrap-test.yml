spring:
  output:
    ansi:
      enabled: DETECT
  application:
    name: reports-service
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  data:
    mongodb:
      uri: "mongodb://localhost:27017/reports"
      
eureka:
  client:
    enabled: false