spring:
  output:
    ansi:
      enabled: DETECT
  application:
    name: reports-service
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
  data:
    mongodb:
      port: 27017
      database: reports