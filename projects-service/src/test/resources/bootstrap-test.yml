spring:
  output:
    ansi:
      enabled: DETECT
  application:
    name: projects-service
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  data:
    mongodb:
      uri: "mongodb://localhost:27017/projects"
      
eureka:
  client:
    enabled: false