spring:
  output:
    ansi:
      enabled: DETECT
  application:
    name: analyses-executor-service
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  data:
    mongodb:
      uri: "mongodb://localhost:27017/analyses-executor"
      
eureka:
  client:
    enabled: false