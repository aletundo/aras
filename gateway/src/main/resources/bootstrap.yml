spring:
  cloud:
    config:
      name: gateway
      # uri: http://config-server:8888
      fail-fast: true
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
